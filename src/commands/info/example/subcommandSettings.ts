import { ISubCommandSettings } from "../../../structures/Interfaces";

const settings: ISubCommandSettings = { 
  maximumArgs: 1,
  permissions: "ADMINISTRATOR",
  guildOnly: true,
};

export default settings;
